server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/employeemanager
    username: david
    password: password
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect